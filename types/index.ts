export type BlogItem = {
    id: string
    title: string
    emoji: string
    date: string
    description: string
}

export type Announcement = {
    id: string
    title: string
    date: string
    description: string
}